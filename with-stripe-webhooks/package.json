{
  "name": "with-stripe-webhooks",
  "scripts": {
    "check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "dev": "neon dev",
    "deploy": "neon deploy --env .env.local",
    "endpoint": "neon functions get stripe"
  },
  "type": "module",
  "dependencies": {
    "@neon/config": "^0.9.1",
    "@neon/env": "^0.11.0",
    "@neon/functions": "^0.5.0",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.4.2",
    "pg": "^8.21.0",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.3"
  }
}
