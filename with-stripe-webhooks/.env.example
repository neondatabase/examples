# From the Stripe Dashboard (Developers → Webhooks) or `stripe listen`.
STRIPE_WEBHOOK_SECRET=whsec_...

# Injected by `neon dev` and written by `neon env pull` from your linked branch.
NEON_BRANCH=
DATABASE_URL=
DATABASE_URL_UNPOOLED=
