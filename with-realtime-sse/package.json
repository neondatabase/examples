{
  "name": "with-realtime-sse",
  "description": "Realtime shared counter using a TanStack Router SPA over server-sent events to a Hono server on Neon Functions, with state stored in Neon Postgres.",
  "private": true,
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@neon/config": "^0.9.1",
    "@neon/env": "^0.11.0",
    "@neon/functions": "^0.8.0",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.4.2",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.3"
  }
}
