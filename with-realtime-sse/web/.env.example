# Base URL of the deployed Neon Function (no trailing slash).
# Local dev with `neonctl dev`:
VITE_FUNCTION_URL="http://localhost:8787"
# Deployed:
# VITE_FUNCTION_URL="https://<branch>-counter.compute.<region>.aws.neon.tech"
